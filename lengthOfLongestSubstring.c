#include <stdio.h>
#include <stdlib.h>

int lengthOfLongestSubstring(char * s){
    int len=0;

    return len;
}

int main(){
    int len;
    char* s = "pwwkew"; // output: 3 -> wke
    len = lengthOfLongestSubstring(s);
    printf("%d", len);
    return len;
}